#include <stdio.h>

/**
 * main - Entry point
 * @argc: size of commands
 * @argv: commands list
 * Return: Always 0
*/

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
